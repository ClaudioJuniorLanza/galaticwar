spring:
  datasource:
    url: jdbc:h2:file:/home/clanca/Documentos/LetsCode/galaticwar/database
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

server:
  port: 8081